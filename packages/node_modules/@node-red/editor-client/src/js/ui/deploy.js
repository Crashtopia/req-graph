/**
 * Copyright JS Foundation and other contributors, http://js.foundation
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 **/

RED.deploy = (function () {
    var exportData = {
        log = "",
        conversation = ""
    }

    var deploymentTypes = {
        "full": { img: "red/images/deploy-full-o.svg" },
        "nodes": { img: "red/images/deploy-nodes-o.svg" },
        "flows": { img: "red/images/deploy-flows-o.svg" }
    }

    var ignoreDeployWarnings = {
        unknown: false,
        unusedConfig: false,
        invalid: false
    }

    var deploymentType = "full";

    var deployInflight = false;

    var currentDiff = null;

    var activeBackgroundDeployNotification;

    function changeDeploymentType(type) {
        deploymentType = type;
        $("#red-ui-header-button-deploy-icon").attr("src", deploymentTypes[type].img);
    }

    /**
     * options:
     *   type: "default" - Button with drop-down options - no further customisation available
     *      label: the text to display - default: "Deploy"
     *   type: "simple"  - Button without dropdown. Customisations:
     *      label: the text to display - default: "Deploy"
     *      icon : the icon to use. Null removes the icon. default: "red/images/deploy-full-o.svg"
     */
    function init(options) {
        options = options || {};
        var type = options.type || "simple";
        var label = "Execute Workflow";

        if (type == "default") {
            $('<li><span class="red-ui-deploy-button-group button-group">' +
                '<a id="red-ui-header-button-deploy" class="red-ui-deploy-button disabled" href="#">' +
                '<span class="red-ui-deploy-button-content">' +
                '<img id="red-ui-header-button-deploy-icon" src="red/images/deploy-full-o.svg"> ' +
                '<span>' + label + '</span>' +
                '</span>' +
                '<span class="red-ui-deploy-button-spinner hide">' +
                '<img src="red/images/spin.svg"/>' +
                '</span>' +
                '</a>' +
                '<a id="red-ui-header-button-deploy-options" class="red-ui-deploy-button" href="#"><i class="fa fa-caret-down"></i><i class="fa fa-lock"></i></a>' +
                '</span></li>').prependTo(".red-ui-header-toolbar");
            const mainMenuItems = [
                { id: "deploymenu-item-full", toggle: "deploy-type", icon: "red/images/deploy-full.svg", label: RED._("deploy.full"), sublabel: RED._("deploy.fullDesc"), selected: true, onselect: function (s) { if (s) { changeDeploymentType("full") } } },
                { id: "deploymenu-item-flow", toggle: "deploy-type", icon: "red/images/deploy-flows.svg", label: RED._("deploy.modifiedFlows"), sublabel: RED._("deploy.modifiedFlowsDesc"), onselect: function (s) { if (s) { changeDeploymentType("flows") } } },
                { id: "deploymenu-item-node", toggle: "deploy-type", icon: "red/images/deploy-nodes.svg", label: RED._("deploy.modifiedNodes"), sublabel: RED._("deploy.modifiedNodesDesc"), onselect: function (s) { if (s) { changeDeploymentType("nodes") } } },
                null
            ]
            if (RED.settings.runtimeState && RED.settings.runtimeState.ui === true) {
                mainMenuItems.push({ id: "deploymenu-item-runtime-start", icon: "red/images/start.svg", label: RED._("deploy.startFlows"), sublabel: RED._("deploy.startFlowsDesc"), onselect: "core:start-flows", visible: false })
                mainMenuItems.push({ id: "deploymenu-item-runtime-stop", icon: "red/images/stop.svg", label: RED._("deploy.stopFlows"), sublabel: RED._("deploy.stopFlowsDesc"), onselect: "core:stop-flows", visible: false })
            }
            mainMenuItems.push({ id: "deploymenu-item-reload", icon: "red/images/deploy-reload.svg", label: RED._("deploy.restartFlows"), sublabel: RED._("deploy.restartFlowsDesc"), onselect: "core:restart-flows" })
            RED.menu.init({ id: "red-ui-header-button-deploy-options", options: mainMenuItems });
        } else if (type == "simple") {
            var icon = 'red/images/deploy-full-o.svg';
            if (options.hasOwnProperty('icon')) {
                icon = options.icon;
            }

            $('<li><span class="red-ui-deploy-button-group button-group">' +
                '<a id="red-ui-header-button-deploy" class="red-ui-deploy-button disabled" href="#">' +
                '<span class="red-ui-deploy-button-content">' +
                (icon ? '<img id="red-ui-header-button-deploy-icon" src="' + icon + '"> ' : '') +
                '<span>' + label + '</span>' +
                '</span>' +
                '<span class="red-ui-deploy-button-spinner hide">' +
                '<img src="red/images/spin.svg"/>' +
                '</span>' +
                '</a>' +
                '</span></li>').prependTo(".red-ui-header-toolbar");
        }

        $('#red-ui-header-button-deploy').on("click", function (event) {
            event.preventDefault();
            executeWorkflow();
        });

        RED.actions.add("core:deploy-flows", save);
        if (type === "default") {
            if (RED.settings.runtimeState && RED.settings.runtimeState.ui === true) {
                RED.actions.add("core:stop-flows", function () { stopStartFlows("stop") });
                RED.actions.add("core:start-flows", function () { stopStartFlows("start") });
            }
            RED.actions.add("core:restart-flows", restart);
            RED.actions.add("core:set-deploy-type-to-full", function () { RED.menu.setSelected("deploymenu-item-full", true); });
            RED.actions.add("core:set-deploy-type-to-modified-flows", function () { RED.menu.setSelected("deploymenu-item-flow", true); });
            RED.actions.add("core:set-deploy-type-to-modified-nodes", function () { RED.menu.setSelected("deploymenu-item-node", true); });
        }

        window.addEventListener('beforeunload', function (event) {
            if (RED.nodes.dirty()) {
                event.preventDefault();
                event.stopImmediatePropagation()
                event.returnValue = RED._("deploy.confirm.undeployedChanges");
                return
            }
        })

        RED.events.on('workspace:dirty', function (state) {
            if (RED.settings.user?.permissions === 'read') {
                return
            }
            $("#red-ui-header-button-deploy").removeClass("disabled");
        });

        RED.comms.subscribe("notification/runtime-deploy", function (topic, msg) {
            var currentRev = RED.nodes.version();
            if (currentRev === null || deployInflight || currentRev === msg.revision) {
                return;
            }
            if (activeBackgroundDeployNotification?.hidden && !activeBackgroundDeployNotification?.closed) {
                activeBackgroundDeployNotification.showNotification()
                return
            }
            const message = $('<p>').text(RED._('deploy.confirm.backgroundUpdate'));
            const options = {
                id: 'background-update',
                type: 'compact',
                modal: false,
                fixed: true,
                timeout: 10000,
                buttons: [
                    {
                        text: RED._('deploy.confirm.button.review'),
                        class: "primary",
                        click: function () {
                            activeBackgroundDeployNotification.hideNotification();
                            var nns = RED.nodes.createCompleteNodeSet();
                            resolveConflict(nns, false);
                        }
                    }
                ]
            }
            if (!activeBackgroundDeployNotification || activeBackgroundDeployNotification.closed) {
                activeBackgroundDeployNotification = RED.notify(message, options)
            } else {
                activeBackgroundDeployNotification.update(message, options)
            }
        });


        updateLockedState()
        RED.events.on('login', updateLockedState)
    }

    function updateLockedState() {
        if (!RED.user.hasPermission('flows.write')) {
            $(".red-ui-deploy-button-group").addClass("readOnly");
            $("#red-ui-header-button-deploy").addClass("disabled");
        } else {
            $(".red-ui-deploy-button-group").removeClass("readOnly");
            if (RED.nodes.dirty()) {
                $("#red-ui-header-button-deploy").removeClass("disabled");
            }
        }
    }

    function getNodeInfo(node) {
        var tabLabel = "";
        if (node.z) {
            var tab = RED.nodes.workspace(node.z);
            if (!tab) {
                tab = RED.nodes.subflow(node.z);
                tabLabel = tab.name;
            } else {
                tabLabel = tab.label;
            }
        }
        var label = RED.utils.getNodeLabel(node, node.id);
        return { tab: tabLabel, type: node.type, label: label };
    }
    function sortNodeInfo(A, B) {
        if (A.tab < B.tab) { return -1; }
        if (A.tab > B.tab) { return 1; }
        if (A.type < B.type) { return -1; }
        if (A.type > B.type) { return 1; }
        if (A.name < B.name) { return -1; }
        if (A.name > B.name) { return 1; }
        return 0;
    }

    function resolveConflict(currentNodes, activeDeploy) {
        var message = $('<div>');
        $('<p data-i18n="deploy.confirm.conflict"></p>').appendTo(message);
        var conflictCheck = $('<div class="red-ui-deploy-dialog-confirm-conflict-row">' +
            '<img src="red/images/spin.svg"/><div data-i18n="deploy.confirm.conflictChecking"></div>' +
            '</div>').appendTo(message);
        var conflictAutoMerge = $('<div class="red-ui-deploy-dialog-confirm-conflict-row">' +
            '<i class="fa fa-check"></i><div data-i18n="deploy.confirm.conflictAutoMerge"></div>' +
            '</div>').hide().appendTo(message);
        var conflictManualMerge = $('<div class="red-ui-deploy-dialog-confirm-conflict-row">' +
            '<i class="fa fa-exclamation"></i><div data-i18n="deploy.confirm.conflictManualMerge"></div>' +
            '</div>').hide().appendTo(message);

        message.i18n();
        currentDiff = null;
        var buttons = [
            {
                text: RED._("common.label.cancel"),
                click: function () {
                    conflictNotification.close();
                }
            },
            {
                id: "red-ui-deploy-dialog-confirm-deploy-review",
                text: RED._("deploy.confirm.button.review"),
                class: "primary disabled",
                click: function () {
                    if (!$("#red-ui-deploy-dialog-confirm-deploy-review").hasClass('disabled')) {
                        RED.diff.showRemoteDiff(null, {
                            onmerge: function () {
                                activeBackgroundDeployNotification.close()
                            }
                        });
                        conflictNotification.close();
                    }
                }
            },
            {
                id: "red-ui-deploy-dialog-confirm-deploy-merge",
                text: RED._("deploy.confirm.button.merge"),
                class: "primary disabled",
                click: function () {
                    if (!$("#red-ui-deploy-dialog-confirm-deploy-merge").hasClass('disabled')) {
                        RED.diff.mergeDiff(currentDiff);
                        conflictNotification.close();
                        activeBackgroundDeployNotification.close()
                    }
                }
            }
        ];
        if (activeDeploy) {
            buttons.push({
                id: "red-ui-deploy-dialog-confirm-deploy-overwrite",
                text: RED._("deploy.confirm.button.overwrite"),
                class: "primary",
                click: function () {
                    save(true, activeDeploy);
                    conflictNotification.close();
                    activeBackgroundDeployNotification.close()
                }
            })
        }
        var conflictNotification = RED.notify(message, {
            modal: true,
            fixed: true,
            width: 600,
            buttons: buttons
        });

        RED.diff.getRemoteDiff(function (diff) {
            currentDiff = diff;
            conflictCheck.hide();
            var d = Object.keys(diff.conflicts);
            if (d.length === 0) {
                conflictAutoMerge.show();
                $("#red-ui-deploy-dialog-confirm-deploy-merge").removeClass('disabled')
            } else {
                conflictManualMerge.show();
            }
            $("#red-ui-deploy-dialog-confirm-deploy-review").removeClass('disabled')
        })
    }
    function cropList(list) {
        if (list.length > 5) {
            var remainder = list.length - 5;
            list = list.slice(0, 5);
            list.push(RED._("deploy.confirm.plusNMore", { count: remainder }));
        }
        return list;
    }
    function sanitize(html) {
        return html.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;")
    }

    function shadeShow() {
        $("#red-ui-header-shade").show();
        $("#red-ui-editor-shade").show();
        $("#red-ui-palette-shade").show();
        $("#red-ui-sidebar-shade").show();
    }
    function shadeHide() {
        $("#red-ui-header-shade").hide();
        $("#red-ui-editor-shade").hide();
        $("#red-ui-palette-shade").hide();
        $("#red-ui-sidebar-shade").hide();
    }
    function deployButtonSetBusy() {
        $(".red-ui-deploy-button-content").css('opacity', 0);
        $(".red-ui-deploy-button-spinner").show();
        $("#red-ui-header-button-deploy").addClass("disabled");
    }
    function deployButtonClearBusy() {
        $(".red-ui-deploy-button-content").css('opacity', 1);
        $(".red-ui-deploy-button-spinner").hide();
    }
    function stopStartFlows(state) {
        const startTime = Date.now()
        const deployWasEnabled = !$("#red-ui-header-button-deploy").hasClass("disabled")
        deployInflight = true
        deployButtonSetBusy()
        shadeShow()
        $.ajax({
            url: "flows/state",
            type: "POST",
            data: { state: state }
        }).done(function (data, textStatus, xhr) {
            if (deployWasEnabled) {
                $("#red-ui-header-button-deploy").removeClass("disabled")
            }
        }).fail(function (xhr, textStatus, err) {
            if (deployWasEnabled) {
                $("#red-ui-header-button-deploy").removeClass("disabled")
            }
            if (xhr.status === 401) {
                RED.notify(RED._("notification.error", { message: RED._("user.notAuthorized") }), "error")
            } else if (xhr.responseText) {
                const errorDetail = { message: err ? (err + "") : "" }
                try {
                    errorDetail.message = JSON.parse(xhr.responseText).message
                } finally {
                    errorDetail.message = errorDetail.message || xhr.responseText
                }
                RED.notify(RED._("notification.error", errorDetail), "error")
            } else {
                RED.notify(RED._("notification.error", { message: RED._("deploy.errors.noResponse") }), "error")
            }
        }).always(function () {
            const delta = Math.max(0, 300 - (Date.now() - startTime))
            setTimeout(function () {
                deployButtonClearBusy()
                shadeHide()
                deployInflight = false
            }, delta);
        });
    }
    function restart() {
        var startTime = Date.now();
        var deployWasEnabled = !$("#red-ui-header-button-deploy").hasClass("disabled");
        deployInflight = true;
        deployButtonSetBusy();
        $.ajax({
            url: "flows",
            type: "POST",
            headers: {
                "Node-RED-Deployment-Type": "reload"
            }
        }).done(function (data, textStatus, xhr) {
            if (deployWasEnabled) {
                $("#red-ui-header-button-deploy").removeClass("disabled");
            }
            RED.notify('<p>' + RED._("deploy.successfulRestart") + '</p>', "success");
        }).fail(function (xhr, textStatus, err) {
            if (deployWasEnabled) {
                $("#red-ui-header-button-deploy").removeClass("disabled");
            }
            if (xhr.status === 401) {
                RED.notify(RED._("deploy.deployFailed", { message: RED._("user.notAuthorized") }), "error");
            } else if (xhr.status === 409) {
                resolveConflict(nns, true);
            } else if (xhr.responseText) {
                RED.notify(RED._("deploy.deployFailed", { message: xhr.responseText }), "error");
            } else {
                RED.notify(RED._("deploy.deployFailed", { message: RED._("deploy.errors.noResponse") }), "error");
            }
        }).always(function () {
            var delta = Math.max(0, 300 - (Date.now() - startTime));
            setTimeout(function () {
                deployButtonClearBusy();
                deployInflight = false;
            }, delta);
        });
    }
    function renderMarkdown(text) {
        var plantUmlRegex = /@startuml[\s\S]*?@enduml/g;
        text = text.replace(plantUmlRegex, function (match) {
            var encoded = encodePlantUml(match);
            if (!encoded) return '<pre>' + match + '</pre>';  // fallback to raw text
            return '<img src="https://www.plantuml.com/plantuml/svg/' + encoded + '" style="max-width:100%;margin:8px 0;">';
        });
        // tables
        var lines = text.split('\n');
        var result = [];
        var inTable = false;
        var tableHtml = '';

        lines.forEach(function (line) {
            if (line.trim().startsWith('|')) {
                if (!inTable) {
                    inTable = true;
                    tableHtml = '<table style="border-collapse:collapse;width:100%;font-size:12px;margin:4px 0;">';
                }
                // skip separator rows like |---|---|
                if (/^\|[\s\-\|:]+\|$/.test(line.trim())) return;

                var cells = line.trim().split('|').filter(function (c) { return c.trim() !== ''; });
                var isHeader = tableHtml.indexOf('<tr>') === -1;
                var tag = isHeader ? 'th' : 'td';
                var style = isHeader
                    ? 'style="background:#1a7bbf;color:white;padding:4px 8px;text-align:left;"'
                    : 'style="padding:4px 8px;border-bottom:1px solid #ddd;"';

                tableHtml += '<tr>' + cells.map(function (c) {
                    return '<' + tag + ' ' + style + '>' + c.trim() + '</' + tag + '>';
                }).join('') + '</tr>';
            } else {
                if (inTable) {
                    tableHtml += '</table>';
                    result.push(tableHtml);
                    inTable = false;
                    tableHtml = '';
                }
                result.push(line);
            }
        });

        if (inTable) {
            tableHtml += '</table>';
            result.push(tableHtml);
        }

        return result.join('\n')
            .replace(/^#{4}\s+(.+)$/gm, '<h4 style="margin:8px 0 4px 0;">$1</h4>')
            .replace(/^#{3}\s+(.+)$/gm, '<h3 style="margin:8px 0 4px 0;">$1</h3>')
            .replace(/^#{2}\s+(.+)$/gm, '<h2 style="margin:8px 0 4px 0;">$1</h2>')
            .replace(/^#{1}\s+(.+)$/gm, '<h1 style="margin:8px 0 4px 0;">$1</h1>')
            .replace(/^---$/gm, '<hr style="border:none;border-top:1px solid #ddd;margin:8px 0;">')
            .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')
            .replace(/\*(.*?)\*/g, '<em>$1</em>')
            .replace(/`(.*?)`/g, '<code style="background:#f0f0f0;padding:1px 4px;border-radius:3px;">$1</code>')
            .replace(/\n/g, '<br>');
    }
    function encodePlantUml(text) {
        try {
            if (!text || typeof text !== 'string') return null;
            if (typeof pako === 'undefined') return null;

            var data = unescape(encodeURIComponent(text));
            var compressed = pako.deflateRaw(data, { level: 9 });
            if (!compressed || compressed.length === 0) return null;

            function encode6bit(b) {
                if (b < 10) return String.fromCharCode(48 + b);
                b -= 10;
                if (b < 26) return String.fromCharCode(65 + b);
                b -= 26;
                if (b < 26) return String.fromCharCode(97 + b);
                b -= 26;
                if (b == 0) return '-';
                if (b == 1) return '_';
                return '?';
            }

            function append3bytes(b1, b2, b3) {
                var c1 = b1 >> 2;
                var c2 = ((b1 & 0x3) << 4) | (b2 >> 4);
                var c3 = ((b2 & 0xF) << 2) | (b3 >> 6);
                var c4 = b3 & 0x3F;
                return encode6bit(c1) + encode6bit(c2) + encode6bit(c3) + encode6bit(c4);
            }

            var r = "";
            for (var i = 0; i < compressed.length; i += 3) {
                if (i + 2 == compressed.length) r += append3bytes(compressed[i], compressed[i + 1], 0);
                else if (i + 1 == compressed.length) r += append3bytes(compressed[i], 0, 0);
                else r += append3bytes(compressed[i], compressed[i + 1], compressed[i + 2]);
            }
            return r;
        } catch (err) {
            console.warn("encodePlantUml failed:", err.message);
            return null;
        }
    }
    function downloadFile(content, filename, mimeType) {
        var blob = new Blob([content], { type: mimeType });
        var url = URL.createObjectURL(blob);
        var a = $('<a>').attr({ href: url, download: filename });
        $('body').append(a);
        a[0].click();
        a.remove();
        URL.revokeObjectURL(url);
    }
    function executeWorkflow() {
        const activeWorkspaceId = RED.workspaces.active();
        const allNodes = RED.nodes.createCompleteNodeSet();
        const subflowIds = new Set(
            allNodes
                .filter(n => n.type?.startsWith("subflow:"))
                .map(n => n.type.slice("subflow:".length))
        );
        const nns = allNodes.filter(function (node) {
            // keep the active tab itself
            if (node.id === activeWorkspaceId) return true;
            // keep nodes belonging to the active tab
            if (node.z === activeWorkspaceId) return true;
            // keep global config nodes (no tab association)
            if (!node.z && node.type !== "tab") return true;
            // nodes inside any defined subflow
            if (subflowIds.has(node.z)) return true;
            return false;
        }); deployButtonSetBusy();
        deployInflight = true;
        shadeShow();
        $.ajax({
            url: "executeWorkflow",
            type: "POST",
            data: JSON.stringify({
                flows: nns,
                apiKeys: {
                    gemini: RED.sidebar.models.getKey("gemini"),
                    mistral: RED.sidebar.models.getKey("mistral")
                }
            }),
            contentType: "application/json; charset=utf-8"
        }).done(function (data) {
            exportData.log = data.log
            exportData.conversation = data.conversation
            if (data.status === "error") {
                RED.notify(data.message, "error");
                return;
            }

            console.log(data.log)
            function openDialog() {
                RED.nodes.dirty(false);
                RED.nodes.version(data.rev);
                RED.nodes.originalFlow(RED.nodes.createCompleteNodeSet());

                var dialog = $('<div>').css({
                    display: 'flex',
                    flexDirection: 'column',
                    height: '100%'
                });

                var chatWindow = $('<div>').css({
                    flex: 1,
                    overflowY: 'auto',
                    padding: '8px',
                    display: 'flex',
                    flexDirection: 'column',
                    gap: '8px'
                });

                if (data.conversation && Array.isArray(data.conversation)) {
                    data.conversation.forEach(function (entry) {
                        var bubble = $('<div>').css({
                            background: '#f0f0f0',
                            borderRadius: '8px',
                            padding: '8px 12px',
                            maxWidth: '80%',
                            alignSelf: 'flex-start'
                        });

                        var agentName = $('<div>').css({
                            fontWeight: 'bold',
                            fontSize: '11px',
                            marginBottom: '4px',
                            color: '#1a7bbf'
                        }).text(entry.agent);

                        var message = $('<div>').css({
                            fontSize: '13px'
                        }).html(renderMarkdown(entry.message));

                        bubble.append(agentName);
                        bubble.append(message);

                        chatWindow.append(bubble);
                    });
                }

                dialog.append(chatWindow);
                if (data.status === "warning") {

                    var warningBox = $('<div>').css({
                        background: '#fff3cd',
                        color: '#856404',
                        border: '1px solid #ffeeba',
                        borderRadius: '4px',
                        padding: '10px',
                        marginBottom: '10px',
                        fontSize: '13px'
                    });
                    data.message.split("\n").forEach(function (warning) {
                        if (warning.trim()) {
                            $('<div>')
                                .text('⚠️ ' + warning.trim())
                                .appendTo(warningBox);
                        }
                    });
                    dialog.append(warningBox);
                }
                if (data.HITL === true) {
                    var inputRow = $('<div>').css({
                        display: 'flex',
                        gap: '4px',
                        paddingTop: '8px',
                        borderTop: '1px solid #ccc'
                    });

                    var textInput = $('<input>', {
                        type: 'text',
                        placeholder: 'Type your feedback...'
                    }).css({ flex: 1 }).addClass('red-ui-typedInput');

                    var acceptBtn = $('<button>').text('Accept').addClass('red-ui-button');
                    var rejectBtn = $('<button>').text('Reject').addClass('red-ui-button').css({ color: '#e74c3c' });

                    function addHumanBubble(text) {
                        var bubble = $('<div>').css({
                            background: '#d4edff',
                            borderRadius: '8px',
                            padding: '8px 12px',
                            maxWidth: '80%',
                            alignSelf: 'flex-end'
                        });
                        bubble.append($('<div>').css({
                            fontWeight: 'bold',
                            fontSize: '11px',
                            marginBottom: '4px',
                            color: '#1a7bbf'
                        }).text('Human'));
                        bubble.append($('<div>').css({ fontSize: '13px' }).text(text));
                        chatWindow.append(bubble);
                    }

                    function handleResume(accepted) {
                        var value = textInput.val().trim();
                        if (!accepted && !value) return;

                        addHumanBubble(accepted ? '✓ Accepted' : value);
                        textInput.val('');

                        var nodeId = accepted ? data.hitlNodeId : data.hitlNodeIdReject;
                        chatWindow.scrollTop(chatWindow[0].scrollHeight);
                        resumeWorkflow(value, data.conversation, nodeId, exportData.log, function (result) {
                            result.conversation.slice(data.conversation.length).forEach(function (entry) {
                                if (entry.agent === "Human") return;
                                var bubble = $('<div>').css({
                                    background: '#f0f0f0',
                                    borderRadius: '8px',
                                    padding: '8px 12px',
                                    maxWidth: '80%',
                                    alignSelf: 'flex-start'
                                });
                                bubble.append($('<div>').css({
                                    fontWeight: 'bold',
                                    fontSize: '11px',
                                    marginBottom: '4px',
                                    color: '#1a7bbf'
                                }).text(entry.agent));
                                bubble.append($('<div>').css({
                                    fontSize: '13px'
                                }).html(renderMarkdown(entry.message)));
                                chatWindow.append(bubble);
                            });

                            data.conversation = result.conversation;
                            data.hitlNodeId = result.hitlNodeId;
                            data.hitlNodeIdReject = result.hitlNodeIdReject;

                            if (!result.hitlNodeId) {
                                inputRow.hide();
                            }

                            chatWindow.scrollTop(chatWindow[0].scrollHeight);
                        });
                    }

                    acceptBtn.on('click', function () { handleResume(true); });
                    rejectBtn.on('click', function () { handleResume(false); });
                    textInput.on('keypress', function (e) {
                        if (e.which === 13) handleResume(false);
                    });

                    inputRow.append(textInput);
                    inputRow.append(acceptBtn);
                    inputRow.append(rejectBtn);
                    dialog.append(inputRow);
                }

                dialog.dialog({
                    title: "Workflow Execution",
                    modal: true,
                    width: 800,
                    height: 600,
                    open: function () {
                        var titleBar = $(this).closest(".ui-dialog").find(".ui-dialog-titlebar");

                        var btnGroup = $('<div>').css({
                            position: 'absolute',
                            right: '40px',
                            top: '50%',
                            transform: 'translateY(-50%)',
                            display: 'flex',
                            gap: '4px'
                        });

                        function makeBtn(icon, tooltip, onClick) {
                            return $('<button>').addClass('red-ui-button')
                                .css({ padding: '2px 6px', fontSize: '12px' })
                                .attr('title', tooltip)
                                .html('<i class="fa ' + icon + '"></i>')
                                .on('click', onClick);
                        }

                        btnGroup.append(makeBtn('fa-download', 'Export conversation as JSON', function () {
                            var json = JSON.stringify(exportData.conversation, null, 2);
                            downloadFile(json, 'conversation.json', 'application/json');
                        }));

                        btnGroup.append(makeBtn('fa-file-text-o', 'Export conversation as Markdown', function () {
                            var md = exportData.conversation.map(function (e) {
                                return '## ' + e.agent + '\n\n' + e.message;
                            }).join('\n\n---\n\n');
                            downloadFile(md, 'conversation.md', 'text/markdown');
                        }));

                        btnGroup.append(makeBtn('fa-flask', 'Export experiment data', function () {
                            var json = JSON.stringify(exportData.log, null, 2);
                            downloadFile(json, 'experiment.json', 'application/json');
                        }));

                        titleBar.css('position', 'relative').append(btnGroup);
                    },
                    buttons: {
                        "Close": function () {
                            $(this).dialog("close");
                        }
                    },
                    buttons: {
                        "Close": function () {
                            $(this).dialog("close");
                        }
                    }
                });
            }
            openDialog();
        }).fail(function (xhr) {
            RED.notify("Execution failed: " + xhr.responseText, "error");
        }).always(function () {
            deployButtonClearBusy();
            shadeHide();
        });
    }
    function resumeWorkflow(humanInput, previousConversation, resumeFromNodeId, previousLog, onResult) {
        const activeWorkspaceId = RED.workspaces.active();
        const allNodes = RED.nodes.createCompleteNodeSet();
        const subflowIds = new Set(
            allNodes
                .filter(n => n.type?.startsWith("subflow:"))
                .map(n => n.type.slice("subflow:".length))
        );
        const nns = allNodes.filter(function (node) {
            // keep the active tab itself
            if (node.id === activeWorkspaceId) return true;
            // keep nodes belonging to the active tab
            if (node.z === activeWorkspaceId) return true;
            // keep global config nodes (no tab association)
            if (!node.z && node.type !== "tab") return true;
            // nodes inside any defined subflow
            if (subflowIds.has(node.z)) return true;
            return false;
        });
        deployButtonSetBusy();
        deployInflight = true;
        shadeShow();
        $.ajax({
            url: "resumeWorkflow",
            type: "POST",
            data: JSON.stringify({
                flows: nns,
                apiKeys: {
                    gemini: RED.sidebar.models.getKey("gemini"),
                    mistral: RED.sidebar.models.getKey("mistral")
                },
                humanInput: humanInput,
                previousConversation: previousConversation,
                resumeFromNodeId: resumeFromNodeId,
                previousLog: previousLog
            }),
            contentType: "application/json; charset=utf-8"
        }).done(function (data) {
            exportData.log = data.log
            exportData.conversation = data.conversation
            if (data.status === "error") {
                RED.notify(data.message, "error");
                return;
            }
            lastExecutionData = data;
            onResult(data);
        }).fail(function (xhr) {
            RED.notify("Resume failed: " + xhr.responseText, "error");
        }).always(function () {
            setTimeout(function () {
                deployInflight = false;
                deployButtonClearBusy();
                shadeHide();
            }, 300);
        });
    }

    function save(skipValidation, force) {
        if ($("#red-ui-header-button-deploy").hasClass("disabled")) {
            return; //deploy is disabled
        }
        if ($("#red-ui-header-shade").is(":visible")) {
            return; //deploy is shaded
        }
        if (!RED.user.hasPermission("flows.write")) {
            RED.notify(RED._("user.errors.deploy"), "error");
            return;
        }
        let hasUnusedConfig = false;
        if (!skipValidation) {
            let hasUnknown = false;
            let hasInvalid = false;
            const unknownNodes = [];
            const invalidNodes = [];

            const isDisabled = function (node) {
                return (node.d || RED.nodes.workspace(node.z)?.disabled);
            };

            RED.nodes.eachConfig(function (node) {
                if (node.valid === undefined) {
                    RED.editor.validateNode(node);
                }
                if (!node.valid && !isDisabled(node)) {
                    invalidNodes.push(getNodeInfo(node));
                }
                if (node.type === "unknown") {
                    if (unknownNodes.indexOf(node.name) == -1) {
                        unknownNodes.push(node.name);
                    }
                }
            });
            RED.nodes.eachNode(function (node) {
                if (!node.valid && !isDisabled(node)) {
                    invalidNodes.push(getNodeInfo(node));
                }
                if (node.type === "unknown") {
                    if (unknownNodes.indexOf(node.name) == -1) {
                        unknownNodes.push(node.name);
                    }
                }
            });
            hasUnknown = unknownNodes.length > 0;
            hasInvalid = invalidNodes.length > 0;

            const unusedConfigNodes = [];
            RED.nodes.eachConfig(function (node) {
                if ((node._def.hasUsers !== false) && (node.users.length === 0) && !isDisabled(node)) {
                    unusedConfigNodes.push(getNodeInfo(node));
                    hasUnusedConfig = true;
                }
            });

            let showWarning = false;
            let notificationMessage;
            let notificationButtons = [];
            let notification;
            if (hasUnknown && !ignoreDeployWarnings.unknown) {
                showWarning = true;
                notificationMessage = "<p>" + RED._('deploy.confirm.unknown') + "</p>" +
                    '<ul class="red-ui-deploy-dialog-confirm-list"><li>' + cropList(unknownNodes).map(function (n) { return sanitize(n) }).join("</li><li>") + "</li></ul><p>" +
                    RED._('deploy.confirm.confirm') +
                    "</p>";

                notificationButtons = [
                    {
                        text: RED._("deploy.unknownNodesButton"),
                        class: "pull-left",
                        click: function () {
                            notification.close();
                            RED.actions.invoke("core:search", "type:unknown ");
                        }
                    },
                    {
                        id: "red-ui-deploy-dialog-confirm-deploy-deploy",
                        text: RED._("deploy.confirm.button.confirm"),
                        class: "primary",
                        click: function () {
                            save(true);
                            notification.close();
                        }
                    }
                ];
            } else if (hasInvalid && !ignoreDeployWarnings.invalid) {
                showWarning = true;
                invalidNodes.sort(sortNodeInfo);

                notificationMessage = "<p>" + RED._('deploy.confirm.improperlyConfigured') + "</p>" +
                    '<ul class="red-ui-deploy-dialog-confirm-list"><li>' + cropList(invalidNodes.map(function (A) { return sanitize((A.tab ? "[" + A.tab + "] " : "") + A.label + " (" + A.type + ")") })).join("</li><li>") + "</li></ul><p>" +
                    RED._('deploy.confirm.confirm') +
                    "</p>";
                notificationButtons = [
                    {
                        text: RED._("deploy.invalidNodesButton"),
                        class: "pull-left",
                        click: function () {
                            notification.close();
                            RED.actions.invoke("core:search", "is:invalid ");
                        }
                    },
                    {
                        id: "red-ui-deploy-dialog-confirm-deploy-deploy",
                        text: RED._("deploy.confirm.button.confirm"),
                        class: "primary",
                        click: function () {
                            save(true);
                            notification.close();
                        }
                    }
                ];
            }
            if (showWarning) {
                notificationButtons.unshift(
                    {
                        text: RED._("common.label.cancel"),
                        click: function () {
                            notification.close();
                        }
                    }
                );
                notification = RED.notify(notificationMessage, {
                    modal: true,
                    fixed: true,
                    buttons: notificationButtons
                });
                return;
            }
        }

        const nns = RED.nodes.createCompleteNodeSet();
        const startTime = Date.now();

        deployButtonSetBusy();
        const data = { flows: nns };
        if (!force) {
            data.rev = RED.nodes.version();
        }

        deployInflight = true;
        shadeShow();
        $.ajax({
            url: "flows",
            type: "POST",
            data: JSON.stringify(data),
            contentType: "application/json; charset=utf-8",
            headers: {
                "Node-RED-Deployment-Type": deploymentType
            }
        }).done(function (data, textStatus, xhr) {
            RED.nodes.dirty(false);
            RED.nodes.version(data.rev);
            RED.nodes.originalFlow(nns);
            if (hasUnusedConfig) {
                let notification;
                const opts = {
                    type: "success",
                    fixed: false,
                    timeout: 6000,
                    buttons: [
                        {
                            text: RED._("deploy.unusedConfigNodesButton"),
                            class: "pull-left",
                            click: function () {
                                notification.close();
                                RED.actions.invoke("core:search", "is:config is:unused ");
                            }
                        },
                        {
                            text: RED._("common.label.close"),
                            class: "primary",
                            click: function () {
                                save(true);
                                notification.close();
                            }
                        }
                    ]
                }
                notification = RED.notify(
                    '<p>' + RED._("deploy.successfulDeploy") + '</p>' +
                    '<p>' + RED._("deploy.unusedConfigNodes") + '</p>', opts);
            } else {
                RED.notify('<p>' + RED._("deploy.successfulDeploy") + '</p>', "success");
            }
            const flowsToLock = new Set()
            // Node's properties cannot be modified if its workspace is locked.
            function ensureUnlocked(id) {
                // TODO: `RED.nodes.subflow` is useless
                const flow = id && (RED.nodes.workspace(id) || RED.nodes.subflow(id) || null);
                const isLocked = flow ? flow.locked : false;
                if (flow && isLocked) {
                    flow.locked = false;
                    flowsToLock.add(flow)
                }
            }
            RED.nodes.eachNode(function (node) {
                ensureUnlocked(node.z)
                if (node.changed) {
                    node.dirty = true;
                    node.changed = false;
                }
                if (node.moved) {
                    node.dirty = true;
                    node.moved = false;
                }
                if (node.credentials) {
                    delete node.credentials;
                }
            });
            RED.nodes.eachGroup(function (node) {
                ensureUnlocked(node.z)
                if (node.changed) {
                    node.dirty = true;
                    node.changed = false;
                }
                if (node.moved) {
                    node.dirty = true;
                    node.moved = false;
                }
            })
            RED.nodes.eachJunction(function (node) {
                ensureUnlocked(node.z)
                if (node.changed) {
                    node.dirty = true;
                    node.changed = false;
                }
                if (node.moved) {
                    node.dirty = true;
                    node.moved = false;
                }
            })
            RED.nodes.eachConfig(function (confNode) {
                if (confNode.z) {
                    ensureUnlocked(confNode.z)
                }
                confNode.changed = false;
                if (confNode.credentials) {
                    delete confNode.credentials;
                }
            });
            // Subflow cannot be locked
            RED.nodes.eachSubflow(function (subflow) {
                if (subflow.changed) {
                    subflow.changed = false;
                    RED.events.emit("subflows:change", subflow);
                }
            });
            RED.nodes.eachWorkspace(function (ws) {
                if (ws.changed || ws.added) {
                    // Ensure the Workspace is unlocked to modify its properties.
                    ensureUnlocked(ws.id);
                    ws.changed = false;
                    delete ws.added
                    if (flowsToLock.has(ws)) {
                        ws.locked = true;
                        flowsToLock.delete(ws);
                    }
                    RED.events.emit("flows:change", ws)
                }
            });
            // Ensures all workspaces to be locked have been locked.
            flowsToLock.forEach(flow => {
                flow.locked = true
            })
            // Once deployed, cannot undo back to a clean state
            RED.history.markAllDirty();
            RED.view.redraw();
            RED.sidebar.config.refresh();
            RED.events.emit("deploy");
        }).fail(function (xhr, textStatus, err) {
            RED.nodes.dirty(true);
            $("#red-ui-header-button-deploy").removeClass("disabled");
            if (xhr.status === 401) {
                RED.notify(RED._("deploy.deployFailed", { message: RED._("user.notAuthorized") }), "error");
            } else if (xhr.status === 409) {
                resolveConflict(nns, true);
            } else if (xhr.responseText) {
                RED.notify(RED._("deploy.deployFailed", { message: xhr.responseText }), "error");
            } else {
                RED.notify(RED._("deploy.deployFailed", { message: RED._("deploy.errors.noResponse") }), "error");
            }
        }).always(function () {
            const delta = Math.max(0, 300 - (Date.now() - startTime));
            setTimeout(function () {
                deployInflight = false;
                deployButtonClearBusy()
                shadeHide()
            }, delta);
        });
    }
    return {
        init: init,
        setDeployInflight: function (state) {
            deployInflight = state;
        }

    }
})();
