module.exports = function(RED) {
    function AgentNode(config) {
        RED.nodes.createNode(this, config);
        const node = this;

        node.on("input", function(msg) {
            msg.payload = `Processed by agent node: ${node.name || ""}`;
            node.send(msg);
        });
    }

    RED.nodes.registerType("agent-node", AgentNode);
}