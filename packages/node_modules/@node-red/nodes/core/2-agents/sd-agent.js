module.exports = function(RED) {
    function SoftwareDeveloperAgentNode(config) {
        RED.nodes.createNode(this, config);
        const node = this;
        node.name = config.name;
        node.seedingPrompt = config.seedingPrompt;
        
        node.on("input", function(msg) {
            msg.payload = `Processed by agent node: ${node.name || ""}`;
            node.send(msg);
        });
    }

    RED.nodes.registerType("Software Developer",SoftwareDeveloperAgentNode);
}