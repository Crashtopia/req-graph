module.exports = function(RED) {
    function HitlNode(config) {
        RED.nodes.createNode(this, config);
        const node = this;
        node.name = config.name;

        node.on("input", function(msg) {
            msg.payload = `Processed by HITL node: ${node.name || ""}`;
            node.send(msg);
        });
    }

    RED.nodes.registerType("hitl-node", HitlNode);
}